# Database Read Configuration for Migmechat (Unix Binary)
# This configuration is used for read operations from the database
#
# IMPORTANT: If you see "Access denied for user 'fusion'@'172.17.0.1'" error,
# run: mysql -u root -p < sql/setup-database-user.sql to fix this.

db.driver=com.mysql.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/fusion?useSSL=false&autoReconnect=true&useUnicode=true&characterEncoding=UTF-8
db.username=fusion
db.password=root123
