# Migmechat Docker Environment Configuration
# Copy this file to .env and fill in your values:
#   cp docker/.env.example docker/.env

# ─── MySQL Configuration ─────────────────────────────────────────────────────
MYSQL_ROOT_PASSWORD=changemeplease
MYSQL_DATABASE=fusion
MYSQL_USER=fusion
MYSQL_PASSWORD=fusionpassword
MYSQL_PORT=3306
# Optional additional users created by init-db/01-init.sh
MYSQL_READONLY_PASSWORD=readonly_change_me
MYSQL_BACKUP_PASSWORD=backup_change_me

# ─── Memcache Configuration ──────────────────────────────────────────────────
MEMCACHE_PORT=11211

# ─── Ice Registry Configuration ──────────────────────────────────────────────
REGISTRY_PORT=10000

# ─── Gateway Configuration ───────────────────────────────────────────────────
# GATEWAY_TYPE: TCP | HTTP | WS
GATEWAY_TYPE=TCP
GATEWAY_PORT=9119
GATEWAY_HOST=0.0.0.0

# ─── Application Settings ────────────────────────────────────────────────────
# LOG_LEVEL: DEBUG | INFO | WARN | ERROR
LOG_LEVEL=INFO
TZ=UTC

# ─── Optional Services ───────────────────────────────────────────────────────
# PhpMyAdmin (enabled with --profile admin)
PHPMYADMIN_PORT=8080

# Nginx reverse proxy (enabled with --profile production)
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
