# Database Write Configuration for Migmechat
# This configuration is used for write operations to the database
#
# IMPORTANT: If you see "Access denied for user 'fusion'@'172.17.0.1'" error,
# run: mysql -u root -p < sql/setup-database-user.sql to fix this.
#
# This configuration uses localhost (127.0.0.1) for direct connections

db.driver=com.mysql.jdbc.Driver
db.url=jdbc:mysql://127.0.0.1:3306/fusion
db.username=fusion
db.password=root123
