all:
  decorators:
    ajax:
      name: common.ajax
      body_class: page
    touch:
      name: common.touch
    midlet:
      name: common.midlet
    corporate:
      name: merchant_corporate_space_blue

home:
  view:
    name: home
  components:
    - common.user
    - registration.mobile_verified ?
    - lookout

lookout_user_submit:
  view:
    name: success
  components:
    - common.user
    - lookout_user

settings:
  view:
    name: settings
  components:
    - common.user
    - lookout_settings
